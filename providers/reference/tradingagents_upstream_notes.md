# TradingAgents Upstream Reference Notes

Original `TradingAgents` is reference-only because `TradingAgents-astock` is the A-share fork used for any future review-provider integration.

Upstream: https://github.com/TauricResearch/TradingAgents

It is intentionally not checked into `external/` as a submodule. Keep only
the upstream architecture notes that are useful for comparing with
`TradingAgents-astock`.

Allowed use:

- Compare upstream architecture and agent debate pattern.

Forbidden use:

- Do not use original `TradingAgents` in runtime while `TradingAgents-astock` is the chosen A-share provider.
